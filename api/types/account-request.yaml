properties:
  username:
    description: 用户名称 （只含有汉字、数字、字母、下划线不能以下划线开头和结尾）
    pattern: "^(?!_)(?!.*?_$)[a-zA-Z0-9_\u4e00-\u9fa5]+$"
    required: true
    minLength: 2,
    maxLength: 36
  password:
    description: 用户密码 （匹配数字及字母）
    pattern: "[a-zA-Z0-9]"
    required: true
    minLength: 6,